{
  "name": "rosaura10",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "v1"
  ],
  "scripts": {
    "dev": "npm run dev --workspace rosaura10-v1",
    "dev:v1": "npm run dev --workspace rosaura10-v1",
    "build": "npm run build --workspace rosaura10-v1 && node scripts/assemble.mjs",
    "serve": "npx --yes serve dist"
  },
  "engines": {
    "node": ">=20"
  }
}
